# Review Notes

- Кейс проверяет устойчивость pipeline на structured rows и registry-driven evidence.
- Здесь важно не только наличие narrative evidence, но и корректный учет `students_total`, `teachers_total` и `local_acts_published`.
- Кейс нужен для следующего шага: калибровка `confidence` при structured-heavy пакетах.
