# Review Notes

- Кейс нужен для проверки `OCR-backed evidence linking` внутри более реалистичного corpus layer.
- OCR-файлы берутся из committed benchmark corpus и используются как pilot surrogate до появления реальных анонимизированных сканов.
- Ожидается, что требования будут закрываться без текстовых evidence-файлов, только за счет OCR и organization profile.
