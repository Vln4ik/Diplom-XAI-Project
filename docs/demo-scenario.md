# Демонстрационный сценарий

Дата актуализации: `2026-05-28`

## 1. Назначение документа

Документ фиксирует, как демонстрировать завершённый `MVP 1` продукта `EvidenceXAI` без смешивания текущих возможностей и будущих задач `MVP 2`.

## 2. Что именно показывается

На demo показывается завершённый сценарий `MVP 1`:

1. вход
2. организация
3. загрузка документов
4. обработка
5. поиск по фрагментам
6. анализ отчёта
7. требования / XAI / риски / матрица
8. генерация
9. export
10. согласование

## 3. Demo dataset

Базовый dataset для MVP 1:

- `rosobrnadzor_sample.txt`
- `rosobrnadzor_evidence_site.txt`
- `organization_profile.json`
- `education_metrics.csv`

Альтернативный быстрый путь для насыщенного UI-demo:

- импортировать пакет из `10` готовых организаций:
  - [samples/demo_organizations/README.md](../samples/demo_organizations/README.md)
  - `./.venv/bin/python backend/scripts/import_demo_organizations.py --user-email admin@example.com`

Рекомендуемые категории:

- нормативный документ
- доказательный документ
- другое
- таблица данных

## 4. Ручной demo-сценарий

1. Открыть `http://localhost:5173`.
2. Войти под тестовым пользователем.
3. Создать или выбрать организацию.
4. При необходимости открыть организацию в modal-форме и нажать `Попробовать из вложений`.
5. Перейти в `Документы`.
6. Загрузить demo dataset.
7. Обработать документы.
8. Выполнить поисковый запрос по фрагментам.
9. Перейти в `Отчёты`.
10. Создать новый отчёт.
11. Включить документы в отчёт.
12. Нажать `Анализ`.
13. Проверить:
    - `Требования`
    - `Матрицу`
    - `XAI-объяснения`
    - `Риски`
14. Выполнить одну ручную корректировку требования или review-действие.
15. Нажать `Генерация`.
16. Открыть `Редактор отчёта`.
17. Выгрузить пакет через единое действие `Скачать`.
18. Отправить отчёт на согласование.
19. Проверить `Уведомления` и `Аудит`.

## 5. Где смотреть результат

- `Документы` — обработка и поиск
- `Требования` — выделенные требования
- `Матрица` — карта доказательств
- `XAI-объяснения` — логика explainability внутри `EvidenceXAI`
- `Риски` — открытые пробелы и рекомендованные действия
- `Редактор отчёта` — сгенерированные разделы
- `Уведомления` и `Аудит` — трассы review-контура

## 6. Что именно подтверждает этот demo

Demo подтверждает, что `MVP 1` завершён как функциональное ядро:

- web flow работает
- документный pipeline работает
- аналитика работает end-to-end
- пользователь видит XAI
- export работает
- базовый контур согласования работает

## 7. Что не нужно смешивать с demo `MVP 1`

В этот demo не нужно искусственно включать как будто уже завершённые функции:

- advanced multimodal vision
- large real-corpus evaluation
- электронная подпись
- external integrations
- iOS client

## 8. Что станет demo-фокусом `MVP 2`

Когда начнётся `MVP 2`, demo будет расширяться в сторону:

- OCR / vision quality
- расширенные кейсы `real_corpus`
- более сильный evidence linking
- более качественные разделы

## 9. Автоматическая проверка

```bash
./.venv/bin/pytest -q backend/tests/test_acceptance_demo_flow.py
cd frontend && npm run e2e
```

## 10. Короткий итог

Этот сценарий предназначен именно для демонстрации завершённого `MVP 1`.  
Будущие версии должны расширять demo, а не подменять им текущую завершённую поставку.
