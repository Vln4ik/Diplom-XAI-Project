apiVersion: 1

providers:
  - name: EvidenceXAI
    orgId: 1
    folder: EvidenceXAI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
